// 15-minute × 20 segment burn timeline
// Wave 1: agent-transcript implementation

use super::message::TranscriptMessage;

pub fn generate_burn_timeline(_messages: &[TranscriptMessage]) -> Vec<u64> {
    // Stub: 20 segments
    vec![0; 20]
}
